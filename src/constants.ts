
import { IconGroup } from './types';

// Fallback initial groups if needed, though now mostly fetching from DB
export const INITIAL_GROUPS: IconGroup[] = [];

// Removed INITIAL_ICONS as we now rely on Supabase
